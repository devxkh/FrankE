all: set icons as Ressource (Visusl studio File Properties)

toolkit: set Generic files

remove/change version :
string uriStr = string.Format( @"{0};component/{1}", this.AssemblyName, this.SourcePath );